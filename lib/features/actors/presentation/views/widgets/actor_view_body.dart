import 'package:flutter/material.dart';

class ActorViewBody extends StatelessWidget {
  const ActorViewBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
